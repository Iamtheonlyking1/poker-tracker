{
  "name": "poker-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Home poker buy-in tracker in rupees. Static, no build, hostable on GitHub Pages.",
  "scripts": {
    "test": "npm run lint:ids && node --test \"tests/*.test.js\"",
    "lint:ids": "! grep -rn \"Math.random().toString(36)\" src/ || (echo 'ad-hoc id mint found — use src/id.js' && exit 1)",
    "serve": "python3 -m http.server 8000"
  }
}
